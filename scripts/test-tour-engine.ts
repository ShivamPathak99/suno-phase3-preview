import assert from "node:assert/strict";import { advanceTour, backTour, currentTourStep, skipMissingTourStep, startTour, type TourStep } from "../src/lib/tour/engine";const steps:TourStep[]=[{id:"a",route:"/",targetSelector:"#a",title:"A",body:"",advanceOn:"next"},{id:"b",route:"/b",targetSelector:"#b",title:"B",body:"",advanceOn:"next"}];const first=startTour(steps,"2026-01-01T00:00:00.000Z");assert.ok(first);assert.equal(currentTourStep(steps,first)?.id,"a");const second=advanceTour(steps,first);assert.ok(second);assert.equal(backTour(steps,second).stepId,"a");assert.equal(skipMissingTourStep(steps,first,false)?.stepId,"b");console.log("P3-T18 tour engine passed.");
