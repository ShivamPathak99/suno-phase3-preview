/**
 * Phase 2 constants will be added here with their implementation tickets.
 * Keeping the module in place now freezes the single source of threshold
 * configuration required by the Phase 2 specification.
 */
export {};
