"use client";
import { useState } from "react";
export function WhyChart({ caption, rows, title, watermark }: { caption: string; rows: Array<{ label: string; value: string }>; title: string; watermark?: string }) { const [asTable, setAsTable] = useState(false); return <figure className="why-chart"><div className="why-chart-head"><h3>{title}</h3><button onClick={() => setAsTable(!asTable)} type="button">{asTable ? "View chart" : "View as table"}</button></div>{asTable ? <table><tbody>{rows.map((row) => <tr key={row.label}><th>{row.label}</th><td>{row.value}</td></tr>)}</tbody></table> : <div className="why-bars" role="img" aria-label={title}>{rows.map((row, index) => <div key={row.label}><span style={{ width: `${Math.max(18, 100 - index * 22)}%` }} /><p>{row.label}: <strong>{row.value}</strong></p></div>)}</div>}{watermark ? <figcaption className="why-watermark">{watermark}</figcaption> : <figcaption>{caption}</figcaption>}</figure>; }
